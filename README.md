EF-Core Minimal API (In-Memory Database)

This project is a simple .NET Minimal API application that uses Entity Framework Core with the In-Memory database provider. It demonstrates how to build a small CRUD-based web API without requiring any external database.
